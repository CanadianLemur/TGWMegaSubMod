French_civil_war = {
	FRA_flee_to_africa = {                      
		allowed = { original_tag = FRA }
		visible = {
		has_country_flag = french_military_government
		has_global_flag = kis_second_treaty_frankfort
		}
		available = {     
		or = {has_war_with = AFL has_war_with = AFA has_war_with = ACF}
		surrender_progress > 0.5
		}
		activation = {
		}
		is_good = yes    
		fire_only_once = yes
		ai_will_do = { factor = 100 } 
		complete_effect = {                   
		add_state_core = 459
		add_state_core = 460
		add_state_core = 513
		add_state_core = 272
		set_capital = 459
		set_country_flag = exiled_france
		if = {
		limit = {not = {controls_state = 19}}
		19 = {controller = {country_event = MSMFrenchCrisis.3}}		}
		else = {16 = {controller = {country_event = MSMFrenchCrisis.3}}}
		add_timed_idea = {
		idea = MSM_FRA_recovering_from_exile
		days = 200
		}
		load_focus_tree = france_exiled
		}
		}
	FRA_begin_offensive = {                      
		allowed = { original_tag = FRA }
		visible = {
		or = {has_war_with = AFL has_war_with = AFA has_war_with = ACF has_war_with = FRP}
		}
		available = {
		not = {has_country_flag = FRA_planning_offensive}
		}
		activation = {
		}
		state_target = yes
		target_trigger = {
		   FROM = {
			   has_state_flag = FRA_unplanned_offensive_flag
			   any_neighbor_state = { is_controlled_by = ROOT }
			   NOT = {is_controlled_by = ROOT}
		   }
	   }
		cost = 20
		days_remove = 20
		is_good = yes    
		fire_only_once = yes
		ai_will_do = { factor = 100 } 
		complete_effect = {set_country_flag = FRA_planning_offensive}
		remove_effect = {
		clr_country_flag = FRA_planning_offensive
		FROM = {
		remove_state_flag = FRA_unplanned_offensive_flag
		remove_province_modifier = {
					static_modifiers = { unplanned_offensive }
					province = {
						all_provinces = yes
					}
				}
		}
	}
	}
	}
French_collapse_opportunities = {
	seize_madagascar = {                      
		allowed = { tag = GER }              
		visible = {
			FRA = {has_idea = MSM_FRA_recovering_from_exile}
			has_global_flag = kis_second_treaty_frankfort
		}
		is_good = yes    
		fire_only_once = yes
		ai_will_do = { factor = 100 } 
		complete_effect = {
		if = {
		limit = {FRA = {owns_state = 543}}
		transfer_state = 543
		}
		if = {
		limit = {FRA = {owns_state = 706}}
		transfer_state = 706
		}
		if = {
		limit = {FRA = {owns_state = 708}}
		transfer_state = 708
		}
		if = {
		limit = {FRA = {owns_state = 713}}
		transfer_state = 713
		}
}
}
	seize_french_somaliland = {                      
		allowed = { tag = GER }              
		visible = {
			FRA = {has_idea = MSM_FRA_recovering_from_exile}
			has_global_flag = kis_second_treaty_frankfort
		}
		is_good = yes    
		fire_only_once = yes
		ai_will_do = { factor = 100 } 
		complete_effect = {
		if = {
		limit = {FRA = {owns_state = 268}}
		transfer_state = 268
		}
}
}
	seize_dahomey = {                      
		allowed = { tag = GER }              
		visible = {
			FRA = {has_idea = MSM_FRA_recovering_from_exile}
			has_global_flag = kis_second_treaty_frankfort
		}
		is_good = yes    
		fire_only_once = yes
		ai_will_do = { factor = 100 } 
		complete_effect = {
		if = {
		limit = {FRA = {owns_state = 776}}
		transfer_state = 776
		}
}
}
	seize_ivory_coast = {                      
		allowed = { tag = GER }              
		visible = {
			FRA = {has_idea = MSM_FRA_recovering_from_exile}
			has_global_flag = kis_second_treaty_frankfort
		}
		is_good = yes    
		fire_only_once = yes
		ai_will_do = { factor = 100 } 
		complete_effect = {
		if = {
		limit = {FRA = {owns_state = 779}}
		transfer_state = 779
		}
}
}
	seize_equatorial_africa = {                      
		allowed = { tag = GER }              
		visible = {
			FRA = {has_idea = MSM_FRA_recovering_from_exile}
			has_global_flag = kis_second_treaty_frankfort
		}
		is_good = yes    
		fire_only_once = yes
		ai_will_do = { factor = 100 } 
		complete_effect = {
		if = {
		limit = {FRA = {owns_state = 660}}
		transfer_state = 660
		}
}
}
	seize_french_guiana = {                      
		allowed = { OR = {	tag = CAN  tag = ENG tag = FRB tag = HOL}}              
		visible = {
			owns_state = 687
			FRA = {has_idea = MSM_FRA_recovering_from_exile}
			has_global_flag = kis_second_treaty_frankfort
		}
		is_good = yes    
		fire_only_once = yes
		ai_will_do = { factor = 100 } 
		complete_effect = {
		if = {
		limit = {FRA = {owns_state = 310}}
		transfer_state = 310
		}
}
}
	seize_french_carribean = {                      
		allowed = { OR = {	tag = CAN  tag = ENG tag = FRB }}              
		visible = {
			owns_state = 308
			FRA = {has_idea = MSM_FRA_recovering_from_exile}
			has_global_flag = kis_second_treaty_frankfort
		}
		is_good = yes    
		fire_only_once = yes
		ai_will_do = { factor = 100 } 
		complete_effect = {
		if = {
		limit = {FRA = {owns_state = 694}}
		transfer_state = 694
		}
}			
}
	seize_st_pierre_and_miquelon = {                      
		allowed = { OR = {	tag = CAN  tag = ENG tag = NEW }}              
		visible = {
			owns_state = 331
			FRA = {has_idea = MSM_FRA_recovering_from_exile}
			has_global_flag = kis_second_treaty_frankfort
		}
		is_good = yes    
		fire_only_once = yes
		ai_will_do = { factor = 100 } 
		complete_effect = {
		if = {
		limit = {FRA = {owns_state = 730}}
		transfer_state = 730
		}
}
}
}